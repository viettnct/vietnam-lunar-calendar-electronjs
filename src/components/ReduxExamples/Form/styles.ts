import { makeStyles } from "@material-ui/core";

export const FormStyle = makeStyles({
    textInput: {
        padding: 10,
        display: 'inline-block',
    },
    buttonSubmit: {
        padding: 10,
    }
});
